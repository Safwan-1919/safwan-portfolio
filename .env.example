# Optional: POST endpoint for the Letter form in the Post Room.
# Leave empty (or rename this file to .env and delete the line) to fall back to
# the visitor's mail client with the message pre-filled.
#
# Works with any service that accepts JSON: Formspree, Basin, Getform, your API.
VITE_CONTACT_ENDPOINT=